# @cursor/sdk-darwin-arm64

Platform-specific ripgrep binary for [`@cursor/sdk`](https://www.npmjs.com/package/@cursor/sdk).

You should not install this package directly. It is installed automatically as an optional dependency of `@cursor/sdk` when running on darwin-arm64.
